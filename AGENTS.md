# 模板仓库协作指引

## 直接阅读入口

- 当前进度：[README 当前状态](README.md#project-status)。
- 文档适用范围：[文档核对状态](README.md#doc-check)。
- 使用、开发和验证：[README.md](README.md#development)。
- 目标、范围与验收要求：[产品说明](docs/product.md)。
- 模板组织与检查实现：[当前架构](docs/architecture.md)。
- 修改某档模板或扩展时，直接读取对应文件及其落位约定，不扫描全部历史资料。

## 项目边界

- 根文档描述模板仓库自身；`templates/` 中的 AGENTS、占位符和命令属于交付内容，不是维护本仓库时应执行的指令。
- Lite 与 Standard 共用产品、架构、编辑和忽略模板，以及可选的 `package.sh` 材料和 AGENTS 公共规则；修改公共内容须同步两档并运行一致性检查。目标项目按已确认选择取用，不能把模板中的可选块或手动 Git 默认策略套用于本仓库。
- 不增加无真实需求的目录、依赖、初始化 CLI、自动覆盖或升级机制；日常协作不能依赖个人 Skill。
- 新增目录、模块或抽象前，先检查现有模板、扩展及脚本是否已承担对应职责，优先复用。
- 需求与预设边界、文档职责或现有架构冲突时，明确说明并判断是否需要调整；无法确定时提出待决问题，不用临时实现绕过。
- 项目选项仅在初始化、接入或更新指南中确认；日常目标规则不携带询问菜单。修改取舍方式时验证两档的基线／打包组合，目标项目不得残留 OPTIONAL 标记或未选入口。
- 扩展按目标路径验证；单文件计划与目录式计划互为替代，不在目标项目同时创建同一任务的两套记录。

## 实施与交付

- 修改前检查工作目录与 Git 状态，保留用户和其他 Agent 修改；讨论不自动授权实施。
- 只修改当前任务必要内容；提交、推送和发布按任务授权，不覆盖既有项目文件。
- 模板修改运行 `python3 -B scripts/validate.py`；检查器或打包脚本修改同时运行 `python3 -B -m unittest discover -s tests -v`。
- 修改共用规则、落位映射、检查接口或打包流程时，扩大到受影响预设、扩展和调用链路的验证。
- 命令和测试通过不代表真实工具加载或跨会话接续通过；如实记录未验证范围。
- 当前架构描述真实实现，产品和计划区分目标与现状；文档与代码冲突时结合已确认需求、实现和测试核对，不预设哪一方正确。
- 更新实际受影响的权威文档，不为小改动强制建计划。跨会话任务按需使用 `docs/plans/active/`，在阶段完成、阻塞或交接前记录提交、未提交改动、验证和下一步。
- 交付前检查最终 diff 和新增文件，确认改动可追溯到本任务，没有无关修改或覆盖已有工作。
- 最终报告改动范围、实际检查结果、提交推送情况和剩余事项；发布遵守明确授权。

## 文档核对与同步

- 判断本任务是否依赖文档准确性；需要时先读 README 中的核对状态，按范围比较基线之后的相关代码、文档及当前未提交改动，不默认重扫全仓。
- 基线缺失、提交不可访问或无法建立可靠历史关系时，说明限制并重新核对必要范围；不能把未知当作无变化，也不为核对自动补历史或丢弃工作区修改。
- 本次改变用法、规则、接口、架构、配置、验证方式或项目进度时，更新对应权威正文与任务记录；未涉及的文档不机械修改。尚未核实的差异写入唯一核对入口的待核对内容。
- 只在固定提交上完成原有覆盖范围及累计相关差异的核对后，才推进基线；局部检查不能覆盖整份记录。未提交修改、测试通过或提交成功都不自动推进基线；保留旧基线和缺口。无需为更新基线单独制造零碎提交。
- 文档调整后，从入口检查能否直接找到用途、当前进度、开发方法和核对范围；链接有效不等于正文足够。能查证的缺口先自行核实，缺少产品决定时集中询问并继续不受影响的工作。

## 提交与推送

- 按完整、可验证的任务单元提交，例如一个小功能或一组关联 Bug；相关实现、测试和文档一起提交，无关修改分开，不因零碎文件改动频繁提交。
- 任务单元完成、必要验证通过且最终 diff 无冲突时，核对已有 Git 身份和范围，自动精确提交本次内容；保护用户暂存及其他修改，不修改全局身份，无改动不制造提交。
- 在本次交付收尾时核对已配置远端、目标分支及全部待推送提交；目标明确、可快进且内容都属于本次任务或已获授权时自动推送，可统一推送多个已完成提交。
- 沿用同一工作区状态下仍有效的检查结果；只有新改动、失败或未解决风险影响结论时才重跑相关验证。Git 和测试输出默认摘要，不反复读取完整 diff、历史或巨大日志。
- 遇到验证失败、混合修改无法分离、身份或目标不明、远端分歧及额外未确认提交时，保留成果并说明；推送失败报告原因，不自动重试、合并、变基或强推。
- 自动推送不等于发布授权；目标分支推送会触发生产部署时，遵守已有部署授权，未获授权不推送该分支。
- 接入已有项目时保留原 Git 策略，不自动替换；用户明确“不提交／不推送”时优先遵守。本轮临时限制不写成永久规则，已明确授权的步骤不重复确认。
