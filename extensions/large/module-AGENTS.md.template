# {{MODULE_NAME}} 模块协作指引

> 示例落位：`packages/<module>/AGENTS.md`。使用实际模块目录，不为模板搬迁代码；落位深度不同须重算相对链接。填充并核对规则后使用，删除本段填写说明。

## 适用范围与入口

- 适用目录与职责：待核实；仅补充本模块特有约束，不复制根规则。
- 项目协作与授权：[根 AGENTS](../../AGENTS.md)。
- 系统边界：[当前架构](../../docs/architecture.md)；已拆领域文档时替换为直接入口。
- 相关实现、接口与测试：核实后填写实际路径，不要求通读其他模块。

## 模块特有约束

- 允许依赖与禁止跨越的边界：待确认；已有权威说明时链接相应条款。
- 公共契约与兼容要求：待确认，说明影响哪些调用方。
- 修改本模块时必须保持的不变量：引用已确认规则，不自行推断架构意图。

## 验证入口

| 场景 | 工作目录与命令或操作 | 前提与通过条件 |
|---|---|---|
| 本模块定向验证 | 尚未配置，不执行 | 待核实 |
| 接口或跨模块变化 | 待核实调用方及集成入口 | 待确认 |

已有开发或测试说明时用直接链接替代命令正文。任务状态与文档核对基线仍使用项目统一入口，不在本文件另建一份记录。

## 工具适用性

记录已核实的工具及局部规则加载方式；尚未实测时明确标记。不能假定所有工具都会自动读取本文件，也不能用局部规则扩大项目授权。存在规则冲突时先说明并依据已确认的项目约定处理。
