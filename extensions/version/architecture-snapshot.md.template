---
type: architecture-snapshot
doc_status: draft
release_ref: "{{RELEASE_REF}}"
---

# {{VERSION}} 发布架构快照

> 放置位置：`docs/versions/<version>/architecture-snapshot.md`。
> 仅在确需保留可直接阅读的快照时创建。填写实际发布依据后设为 `archived`，此后不随新版本维护。

## 发布标识与来源

- 所属版本：[版本交付记录](README.md)。
- `release_ref`：填写实际发布 tag 或提交，不使用移动分支替代。
- 源文档：该发布点的 `docs/architecture.md`，并核实与代码一致。
- 当前版本导航：[当前架构](../../architecture.md)，它不是本快照的历史证据。

## 发布时的架构

从发布点复制经核实的架构概览、模块边界、关键数据流与限制；不要从当前分支推断历史。

历史源码路径以“仓库路径 + release_ref”标识，或指向真实固定提交页面，避免伪装为当前文件链接。复制来的相对链接必须重算；历史已删除文件不应链接到当前不存在的位置。

## 发布时的已知限制

待填写该版本当时存在的缺口，不把新版的修复结果写进旧快照。

## 勘误

无。必要勘误说明原因、证据及范围，不能静默改变当时的历史事实。
