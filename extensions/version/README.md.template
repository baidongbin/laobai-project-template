---
type: version
doc_status: draft
version: "{{VERSION}}"
version_status: planned
---

# {{VERSION}} 版本目标与交付

> 放置位置：`docs/versions/<version>/README.md`。只在确需独立版本规划或重大演进时使用，普通小迭代不强制创建。

## 目标与范围

- 产品依据：[产品说明](../../product.md)。
- 当前实现：[当前架构](../../architecture.md)。
- 本版本目标：待填写。
- 明确不做：待填写。
- 对应路线图：实际存在后再添加相对链接。

## 版本基线与验收条件

- 计划从哪个发布或提交演进：待核实。

| 里程碑 | 完成条件 | 实际状态或证据 |
|---|---|---|
| 待填写 | 待填写可观察结果 | 尚未验证 |

## 设计与任务入口

目标设计确有需要时创建同目录 `design.md` 并添加链接。任务继续放在 `docs/plans/`，这里只链接相关任务，不维护第二份详细步骤。

| 工作范围 | 任务主记录 |
|---|---|
| 尚未创建 | 无 |

## 兼容、迁移与风险

待填写实际影响；不涉及则注明不适用。部署与数据迁移的执行必须另有明确授权。

## 交付总结

尚未交付。发布后填写实际 tag／提交、版本范围、验收证据、已知限制与未完成内容。

历史架构默认从对应 tag 的 `docs/architecture.md` 回溯。只有明确需要当前工作区直接阅读时才创建 `architecture-snapshot.md`；不为每个版本复制全部长期文档。

## 后续维护

版本生命周期仅在 front matter 的 `version_status` 维护：`planned`、`in_progress`、`released`、`cancelled`。发布不自动证明所有远期产品目标已完成；取消需要说明原因。
