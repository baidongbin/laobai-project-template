---
id: "PLAN-{{TASK_ID}}"
type: plan
doc_status: draft
task_status: planned
---

# {{TASK_TITLE}}

> 放置位置：`docs/plans/active/<task>.md`。
> 任务状态只在本主记录维护；草稿不是实施授权。完成或取消后迁到同层级 `archive/`，保留真实结束状态。

## 目标与非目标

- 本次目标：待填写。
- 不在本次范围内：待填写。
- 授权边界与有风险的操作：待确认；不因存在计划就自动执行。

## 直接依据与入口

- 需求依据：[产品说明](../../product.md)；已有详细规格时改为直接链接规格与条款号。
- 当前约束：[当前架构](../../architecture.md)。
- 工程入口：[项目 README](../../../README.md)；填写时优先补充直接的开发与验证入口。
- 所属版本／目标设计：只在实际存在且与任务有关时补充相对链接，不为了计划新建版本。
- 相关代码：待检查后填写实际仓库路径。

## 验收条件

| 编号 | 条件与可观察结果 | 验证方式 |
|---|---|---|
| AC-01 | 待填写 | 待填写具体检查或运行场景 |

## 实施前状态与方案

- 当前分支及代码状态：待核实；有未提交修改时说明归属和影响。
- 问题复现或可验证失败条件：待填写；非 Bug 任务可以注明不适用。
- 最小改动方案：待填写。
- 关键假设、依赖或兼容风险：待确认。

最少保留目标与验收、直接入口、具体实施步骤、验证记录和接续信息；其他章节仅在与任务有关时保留。

## 跨模块协作（按需）

| 模块及改动范围 | 输入、产出与契约入口 | 前置依赖 | 执行或集成角色 |
|---|---|---|---|
| 待核实 | 待确认 | 无依赖也需核实 | 实际分工后填写 |

- 实施与集成顺序：待确认，说明哪些可独立推进、哪些须等待接口或前置结果。
- 集成验收：补充到上方 AC 表，包含跨模块关键链路、兼容要求和失败行为；不另建第二套验收表。
- 子任务：只有确需独立执行时才建立并链接；各自维护自己的状态，本主记录只记录整体阻塞和下一步。
- 工作区及资源隔离：有并行需求时链接实际环境说明，不默认启动多 Agent。

仅单模块任务时删除本节；仍使用本文件的任务 ID、状态、验证和归档规则。

## 实施步骤与进度

- [ ] 核对相关实现、需求与验证入口。
- [ ] 完成本次范围内的最小改动。
- [ ] 执行定向验证，记录真实结果及未覆盖部分。
- [ ] 同步受影响的长期文档并完成交付。

将上述通用步骤替换为本任务可执行、可验证的具体步骤。步骤完成不自动代表验收通过；无关步骤删除，复杂任务按实际需要调整，不堆机械勾选项。

## 验证记录

验证对象：待填写 tag、提交或“某提交 + 当前未提交改动”；不能把 HEAD 当作整个脏工作区。

| 验收项 | 实际工作目录、命令或步骤 | 结果 | 证据与限制 |
|---|---|---|---|
| AC-01 | 尚未执行 | 未验证 | 无 |


本文件同时承载计划、进度、验证与交接。确实过长时才升级为同名任务目录，保持任务 ID 并修复相对链接。

## 文档影响

| 变化 | 应更新的唯一权威位置 | 处理情况 |
|---|---|---|
| 待填写本次实际影响；没有则注明无影响 | 待填写相对链接 | 未处理或说明无需更新 |

## 接续信息与下一步

- 最近接续记录：待填写日期、分支及提交；没有 Git 时如实注明。
- 当前未提交改动：待核实相关路径、用途和归属；无则写“无”。
- 核对要求：继续前比较当前工作区与本记录，只复核影响任务的差异。

- 阻塞：待核实；无阻塞时明确写“无”。
- 下一步：待填写新会话能直接执行的具体动作。
- 会影响下一步的已确认结论：待填写；需要长期保留的决策应进入相应文档。

## 结束记录

尚未结束。结束时说明完成或取消、实际验收结论、未验证项及交付位置；按授权记录 commit／push，不编造提交结果。

只有约定的必要验收通过或获得明确的范围调整后，才标记 `done`；残留限制必须保留。取消标记 `cancelled`，不伪装成完成。归档时 `doc_status` 改为 `archived`，并修复所有受影响的链接。
