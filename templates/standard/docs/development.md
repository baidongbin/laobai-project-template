---
type: development
doc_status: draft
---

# 开发、运行与调试

> 定位：对应当前分支实际可用的开发方式。尚未核实的环境或命令不得描述为可用。

## 环境要求

| 项目 | 版本或要求 | 事实来源 |
|---|---|---|
| 运行时 | 待核实 | 待填写实际依赖文件或已核实的要求 |
| 依赖管理工具 | 待核实 | 待填写锁定文件与安装方式 |
| 外部服务 | 待核实；无需则注明 | 待填写本地替代或连接限制 |

## 配置

| 配置名 | 用途 | 是否必需 | 安全示例或提供方式 |
|---|---|---|---|
| 待填写 | 待填写 | 待确认 | 不填写 Token、密码、生产地址等敏感值 |

实际使用环境变量时再增加 `.env.example`，只保存安全占位值。个人配置和机器绝对路径不作为项目公共依赖。

## 依赖、启动与构建

| 用途 | 工作目录 | 可执行命令 | 前置条件 | 成功判据 |
|---|---|---|---|---|
| 安装依赖 | 待填写 | 待配置 | 待填写 | 待填写 |
| 本地启动 | 待填写 | 待配置 | 待填写 | 待填写 |
| 构建 | 待填写 | 待配置或注明不适用 | 待填写 | 待填写 |
| 安全停止本地进程 | 待填写 | 待配置 | 确认是当前项目进程 | 待填写 |

已有原生命令就直接使用；只有确需编排时增加 `scripts/`。不要为模板补写无实际依据的脚本或把占位文本当命令运行。

## 环境重建与隔离

按实际需要说明：依赖版本与锁定文件、本地配置来源、外部服务启动或替代方式、测试数据准备与清理。已有命令直接引用上面的入口，不维护第二套安装流程；敏感值只说明提供渠道。

从仅具备上述必要工具的环境开始，按文档执行安装、配置、启动和定向验证，记录实际验证环境与限制；未执行过就注明“环境重建未验证”，不能因本机可运行而认定可重建。

涉及多人、多工作区或多 Agent 并行时，再填写端口、数据、输出及外部资源的隔离方法。不假设新工作区自动隔离共享服务，不为模板增加容器或初始化脚本。

## 调试入口

待填写实际日志位置、调试启动方式、关键排错入口。说明可能出现的隐私数据及脱敏要求。

## 常见问题

| 症状 | 已确认原因或定位方式 | 安全处理方式 |
|---|---|---|
| 待填写真实问题 | 待填写；猜测需标为待验证 | 待填写 |

## 关联资料

- 实现与模块入口：[当前架构](architecture.md)。
- 验证方式：[测试说明](testing.md)。
- 用户入口：[项目 README](../README.md)。

## 打包

在项目根目录执行 `sh package.sh`（需要 Git；Windows 使用 Git Bash/WSL）。

[package.sh](../package.sh) 将已提交的 HEAD 打成根目录下的 `<项目名>-<短HEAD>.zip`，方便上传给 AI 或分享。未提交内容不包含在内；同名包直接替换，成功后清理根目录中本项目的其他普通旧包，只保留最新包；打包失败时保留旧包。
