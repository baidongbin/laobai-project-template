---
type: architecture
doc_status: draft
---

# 当前架构

> 定位：仅描述当前检出分支已经实现的结构，不代表生产状态，也不代表目标版本已经完成。
> 事实基线：尚未核对项目代码。先检查实际目录、依赖和入口，再填写；未实现部分只能放在“已知缺口”或目标设计中。

## 架构概览

待填写：系统边界、主要组成与关键协作方式。无业务代码时明确写“尚未实现”，不要从模板推断架构。

## 模块与代码入口

| 模块 | 当前职责 | 实际代码位置 | 不承担的职责 |
|---|---|---|---|
| 待核实 | 待核实 | 待填写仓库相对路径 | 待核实 |

路径存在后再写成 Markdown 链接；从本文件引用仓库根路径时使用 `../`。

## 关键调用与数据流

待填写一条真实关键链路：入口 → 校验或调度 → 业务处理 → 数据或外部系统 → 返回结果。

说明关键状态、错误处理和边界；只有真实需要时再补图，不为每个函数建立文档映射。

## 数据、接口与依赖边界

| 边界或依赖 | 当前用途 | 契约或实现入口 | 失败或兼容约束 |
|---|---|---|---|
| 待核实 | 待核实 | 待填写 | 待确认 |

## 重要约束与取舍

待填写已确认且仍适用的约束，以及选择当前方案的原因。少量决策直接写在这里；需要长期独立追溯时，才创建 `decisions/` 并在此链接结论。

## 关键不变量与验证

| 必须保持的约束及适用范围 | 原因或决策依据 | 实现与验证入口 | 未覆盖部分 |
|---|---|---|---|
| 待核实；只写已确认且仍适用的约束 | 待填写 | 实际存在后链接代码、测试或检查命令 | 待核实 |

优先记录跨模块依赖、关键数据或状态的一致性要求。与上一节相同的理由用引用串联，不复制正文；不根据模板预设分层架构。没有已确认不变量时如实说明。尚无自动检查时注明人工核验方式或待验证项，不把约束声明当作已验证。

## 已知缺口与过渡状态

| 实际缺口或过渡状态 | 影响 | 已确认的处理方向 |
|---|---|---|
| 待核实 | 待确认 | 未确定；不是自动实施授权 |

代码与目标设计不一致时分别记录。过渡架构如实描述，不能提前改写为完整目标架构。

## 关联资料

- 目标与业务规则：[产品说明](product.md)。
- 工程入口：[项目 README](../README.md)。
- 版本设计、重要决策：只链接与当前架构直接相关且实际存在的资料。
